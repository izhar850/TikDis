specVersion: "v1"
serviceId: "tickdis"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tickdis-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
